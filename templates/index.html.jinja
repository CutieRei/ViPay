<!DOCTYPE html>
<html>
	<head>
		<meta name="viewport" content="width=device-width, initial-scale=1">
	</head>
	<body>
		{% if user %}
			<h1>Hello {{ user.username }}</h1>
			<p>Your balance is Rp.{{ "{0:,}".format(user.balance) }}k</p>
		{% else %}
			<h1>Hello</h1>
		{% endif %}
	</body>
</html>
